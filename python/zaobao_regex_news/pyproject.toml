[project]
name = "zaobao-regex-news"
version = "0.1.0"
description = "Fetch zaobao.com and print five news titles using regex."
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

